---
import Site from "../layouts/Site.astro";
import AnimatedCard from "../components/AnimatedCard.tsx";
import FadeIn from "../components/FadeIn.tsx";
import ProjectCard from "../components/ProjectCard.astro";
import { projects } from "../data/projects";

const description =
  "Jakub Honzal je full-stack web developer se smyslem pro design. Vytváří přehledné, moderní a funkční webové projekty v Astu, Reactu a TypeScriptu.";
const siteUrl = Astro.site?.toString() ?? "https://portfolio-t1f.pages.dev/";
const structuredData = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: "Jakub Honzal – Web developer",
  url: siteUrl,
  description,
  inLanguage: "cs",
  author: {
    "@type": "Person",
    name: "Jakub Honzal",
  },
  potentialAction: {
    "@type": "ContactAction",
    target: `${siteUrl}#contact`,
    name: "Kontaktujte Jakuba Honzala",
  },
};
---
<Site title="Jakub Honzal – Web developer" description={description} structuredData={structuredData}>
  <!-- Hero -->
  <section id="home" class="relative h-[80vh] flex items-center justify-center overflow-hidden">
    <FadeIn client:load>
      <div class="relative z-10 text-center">
        <h1 class="font-display text-5xl md:text-7xl leading-tight text-paper">
          Tvořím estetické a funkční weby
          <span class="block text-gradient">s důrazem na detail.</span>
        </h1>

        <p class="mt-6 max-w-2xl mx-auto text-lg text-white/80">
          Full-stack developer se smyslem pro design. Vytvářím přehledné a moderní weby.
        </p>

        <div class="mt-8 flex gap-4 justify-center">
          <a href="#projects" class="px-5 py-3 rounded-xl btn-gradient text-white font-medium shadow-glow hover:scale-105 transition-transform">Projekty</a>
          <a href="#contact" class="px-5 py-3 rounded-xl border border-white/25 hover:border-white transition backdrop-blur-sm bg-white/5">Kontakt</a>
        </div>
      </div>
    </FadeIn>
  </section>

  <!-- Services -->
  <section id="services" class="py-20">
    <div class="container">
      <h2 class="text-center font-display text-4xl mb-12">Co dělám</h2>
      <div class="grid md:grid-cols-3 gap-6">
        <AnimatedCard client:visible>
          <div class="p-6 rounded-2xl border border-white/10 hover:border-white/30 transition h-full bg-white/5 backdrop-blur-sm">
            <h3 class="font-display text-xl">Rychlost</h3>
            <p class="mt-2 text-white/75">Astro, optimalizace, čistý kód.</p>
          </div>
        </AnimatedCard>
        <AnimatedCard client:visible>
          <div class="p-6 rounded-2xl border border-white/10 hover:border-white/30 transition h-full bg-white/5 backdrop-blur-sm">
            <h3 class="font-display text-xl">Estetika</h3>
            <p class="mt-2 text-white/75">Typografie, whitespace, konzistence.</p>
          </div>
        </AnimatedCard>
        <AnimatedCard client:visible>
          <div class="p-6 rounded-2xl border border-white/10 hover:border-white/30 transition h-full bg-white/5 backdrop-blur-sm">
            <h3 class="font-display text-xl">Funkce</h3>
            <p class="mt-2 text-white/75">Formuláře, chat, video, integrace.</p>
          </div>
        </AnimatedCard>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects" class="py-20">
    <div class="container">
      <h2 class="text-center font-display text-4xl mb-12 text-gradient">Projekty</h2>
      <FadeIn client:visible>
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {projects.map((p) => (
            <ProjectCard p={p} />
          ))}
        </div>
      </FadeIn>
    </div>
  </section>

  <!-- About -->
  <section id="about" class="py-20">
    <div class="container max-w-3xl">
      <h2 class="font-display text-4xl text-gradient">O mně</h2>
      <FadeIn client:visible>
        <p class="mt-2 text-white/80">
          Jmenuji se Jakub a věnuji se webovému i aplikačnímu vývoji. K počítačům mám blízko od dětství – od jednoduchých programů a menších her v Pythonu jsem se postupně posunul k webům a aplikacím, kde mě baví spojení technické i vizuální stránky.
        </p>

        <p class="mt-4 text-white/80">
          Rád dělám fullstack vývoj, protože mi dává úplnou kontrolu nad tím, co se na stránkách děje a jak jednotlivé části fungují. Ke svému vzdělání v tomto oboru se aktivně učím dál – dělám kurzy, pracuji na vlastních projektech a neustále si vyhledávám nové informace, které mě posouvají dál.
        </p>

        <p class="mt-4 text-white/80">
          Zaměřuji se na tvorbu přehledných, funkčních a estetických webů. Aktuálně pracuji na portálu pro podnikání v oblasti investičních a realitních služeb, kde řeším jak backend a frontend, tak i design. Současně vyvíjím vlastní aplikaci <strong>MedAI</strong>, která pomáhá lidem sledovat zdravotní a fitness cíle s podporou umělé inteligence.
        </p>

        <h3 class="font-display text-2xl mt-10">Moje technologie a nástroje</h3>
        <ul class="mt-4 space-y-2 opacity-90">
          <li><strong>Frontend:</strong> HTML, CSS, JavaScript, TypeScript, Astro</li>
          <li><strong>Backend:</strong> JavaScript (Node.js), Python, SQL</li>
          <li><strong>Design &amp; nástroje:</strong> Figma, VS Code, PyCharm</li>
        </ul>

        <p class="mt-8 text-white/80">
          Mám rád výzvy, učím se rychle a problémy beru jako příležitost, jak posunout svoje znalosti dál. Dbám na detaily, protože věřím, že právě ty dělají rozdíl mezi průměrnou a profesionální prací.
        </p>
      </FadeIn>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="py-20">
    <div class="container max-w-2xl">
      <h2 class="font-display text-4xl text-center mb-6 text-gradient">Kontakt</h2>
      <p class="text-center text-white/75">Ozvi se ohledně spolupráce, rád odpovím.</p>
      <FadeIn client:visible>
        <form action="https://formspree.io/f/meollynb" method="POST" class="mt-8 grid gap-4">
          <input name="name" placeholder="Jméno" class="px-4 py-3 rounded-xl border border-white/15 bg-white/5 text-white placeholder-white/50 focus:outline-none focus:border-mint focus:ring-2 focus:ring-mint/40" required />
          <input type="email" name="email" placeholder="Email" class="px-4 py-3 rounded-xl border border-white/15 bg-white/5 text-white placeholder-white/50 focus:outline-none focus:border-mint focus:ring-2 focus:ring-mint/40" required />
          <textarea name="message" placeholder="Zpráva" rows="5" class="px-4 py-3 rounded-xl border border-white/15 bg-white/5 text-white placeholder-white/50 focus:outline-none focus:border-mint focus:ring-2 focus:ring-mint/40" required></textarea>
          <button class="px-5 py-3 rounded-xl btn-gradient text-white font-medium shadow-glow hover:scale-105 transition-transform">Odeslat</button>
        </form>
      </FadeIn>
    </div>
  </section>
</Site>
