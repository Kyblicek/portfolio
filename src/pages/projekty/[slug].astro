---
import Site from "../../layouts/Site.astro";
import { projects } from "../../data/projects";

export function getStaticPaths() {
  return projects.map((p) => ({
    params: { slug: p.slug },
    props: { p },
  }));
}

const { p } = Astro.props;
---
<Site title={`${p.title} – Jakub Honzal`}>
<article class="py-16 max-w-3xl">
<a href="/projekty" class="text-purple">← Zpět na projekty</a>
<h1 class="font-display text-4xl md:text-5xl mt-4">{p.title}</h1>
<p class="mt-2 opacity-70">{p.year} · {p.stack.join(" · ")}</p>
{p.cover && <img src={p.cover} alt={p.title} class="mt-6 rounded-2xl" />}
<div class="prose mt-6">
<p>{p.summary}</p>
<p>Sem doplň delší case‑study, screenshoty, architekturu atd.</p>
{p.url && <p><a class="text-purple" href={p.url} target="_blank">Otevřít projekt →</a></p>}
</div>
</article>
</Site>